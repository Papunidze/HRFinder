import PageLayout from "@/layout/pageLayout";
import MembersForm from "@/modules/[members]/form/[...members]-form";

const User = () => {
  return (
    <PageLayout>
      <MembersForm />
    </PageLayout>
  );
};

export default User;
